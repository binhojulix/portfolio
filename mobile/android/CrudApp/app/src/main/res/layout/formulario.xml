<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="crud.binho.com.br.crudapp.FormularioActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin">

        <ImageView
            android:src="@android:drawable/ic_menu_camera"
            style="@style/formulario_wrap"
            android:id="@+id/img_beer"/>

        <TextView
            style="@style/formulario_match"
            android:text="@string/nome" />

        <EditText
            style="@style/formulario_match"
            android:hint="@string/msg_nome"
            android:id="@+id/edt_nome"/>

        <TextView
            style="@style/formulario_match"
            android:text="@string/valor" />

        <EditText
            style="@style/formulario_match"
            android:hint="@string/msg_valor"
            android:id="@+id/edt_valor"/>

        <TextView
            style="@style/formulario_match"
            android:text="@string/nota" />

        <RatingBar
            style="@style/formulario_wrap"
            android:numStars="5"
            android:id="@+id/rt_nota"/>


        <Button
            style="@style/formulario_match"
            android:text="@string/salvar"
            android:id="@+id/btn_salvar"/>


    </LinearLayout>


</ScrollView>
